{
  "name": "social-block",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build && vite build --config vite.content.config.ts",
    "lint": "oxlint",
    "preview": "vite preview",
    "check": "node src/match.check.ts && node src/release.check.ts",
    "package": "test -z \"$(git status --porcelain)\" && npm run check && npm run build && mkdir -p release && (cd dist && zip -qr ../release/social-block-$npm_package_version.zip .) && git archive --format=zip --prefix=social-block/ -o release/social-block-$npm_package_version-sources.zip HEAD"
  },
  "dependencies": {
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.2",
    "@types/node": "^24.13.2",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.3",
    "oxlint": "^1.71.0",
    "typescript": "~6.0.2",
    "vite": "^8.1.1"
  },
  "engines": {
    "node": ">=22"
  }
}
